// pub fn sigmoid(ctx: &OpenCLContext/*, a: &SharedTensor<f32>, result: &SharedTensor<f32>*/) {
//     let k = ctx.kernels().sigmoid();
//     k.set_arg(0, a);
//     k.set_arg(1, result);

//     let event = ctx.queue.enqueue_kernel()
// }